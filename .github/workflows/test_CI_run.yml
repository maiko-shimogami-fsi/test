name: form-example 
on:
 workflow_dispatch:
    inputs:
      input_mod:
        description: 'Please enter the SHA-1 or branch name to UnitTest.'
        required: false
      input_compo:
        description: 'Please enter the compo name to UnitTest.'
        required: false
 
jobs:
    check-bats-version1:
      runs-on: ubuntu-latest
     
      steps:
      #環境を設定する
        - name: create text
          uses: actions/checkout@v3
        - run: |
             echo "aaa"
             
      #チェック確認
        - name: check1
          run: |
             echo "check-bats-version1を実施しました"

    check-bats-version2:
      runs-on: windows-latest
      needs: check-bats-version1
     
      steps:
      #環境を設定する
        - name: create text
          uses: actions/checkout@v3
        - run: |
             echo "bbb_flg=false" >> $GITHUB_ENV
             
      #チェック確認
        - name: check2
          if: ( (github.event.inputs.input_mod != '') && ( github.event.inputs.input_compo != ''))
          run: |
            echo "aaa_flg=true" >> $GITHUB_ENV

        - name: check3
          run: |
            echo "env.bbb_flg=${env.bbb_flg}"
            #if [ $setting_errstatus ] || [ $aaa_errstatus ]; then
             # 実行を終了させる
            # echo "エラー終了します"
            #fi
