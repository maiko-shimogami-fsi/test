name: form-example 
on:
 workflow_dispatch:
    inputs:
      input_mod:
        description: 'Please enter the SHA-1 or branch name to UnitTest.'
        required: false
      input_compo:
        description: 'Please enter the compo name to UnitTest.'
        required: false
 
jobs:
    check-bats-version1:
      runs-on: ubuntu-latest
     
      steps:
      #環境を設定する
        - name: create text
          uses: actions/checkout@v3
        - run: |
             echo "aaa"
             
      #チェック確認
        - name: check1
          run: |
             echo "check-bats-version1を実施しました"

    check-bats-version2:
      runs-on: windows-latest
      needs: check-bats-version1
     
      steps:
      #環境を設定する
        - name: create text
          uses: actions/checkout@v3
        - run: |
             setting_errstatus=ture
             echo "setting_errstatus=$setting_errstatus" 
             #echo "setting_errstatus=$setting_errstatus" >> $GITHUB_OUTPUT
             
      #チェック確認
        - name: check2
          if: ( (github.event.inputs.input_mod != '') && ( github.event.inputs.input_compo != ''))
          run: |
            aaa_errstatus=ture
            echo "aaa_errstatus=$aaa_errstatus"
            #echo "aaa_errstatus=$aaa_errstatus" >> $GITHUB_OUTPUT

        - name: check3
          run: |
            echo "setting_errstatus=$setting_errstatus" 
            echo "aaa_errstatus=$aaa_errstatus"
            #if [ $setting_errstatus -eq 'ture' ] || [ $aaa_errstatus -eq 'ture' ]; then
            # # 実行を終了させる
            # echo "エラー終了します"
            #fi
