name: form-example 
on:
 workflow_dispatch:
    inputs:
      input_node:
        description: 'Please enter the node name.'
        required: true
  
jobs:
    check-bats-version:
      runs-on: ubuntu-20.04

      steps:
      #環境を設定する
        - name: create text
          uses: actions/checkout@v3
        - run: |

      #チェック確認
        - name: check
          run: |
           cd tools
           chmod 777 check_2.sh
           sh ./check_2.sh
      
