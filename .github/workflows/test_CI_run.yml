name: form-example 
on:
 workflow_dispatch:
    inputs:
      input_node:
        description: 'Please enter the node name.'
        required: false
 
jobs:
    check-bats-version:
      runs-on: ubuntu-20.04
     
      steps:
      #環境を設定する
        - name: create text
          uses: actions/checkout@v3
        - run: |

      #チェック確認
        - name: check1
          run: |
            if [[ -z "${{inputs.input_node}}" ]]; then
             echo "空です"
            else
             echo "空じゃないです"
            fi
            
