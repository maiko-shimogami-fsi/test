name: form-example 
on:
 workflow_dispatch:
    inputs:
      input_mod:
        description: 'Please enter the SHA-1 or branch name to UnitTest.'
        required: false
      input_compo:
        description: 'Please enter the compo name to UnitTest.'
        required: false
 
jobs:
    check-bats-version1:
      runs-on: ubuntu-latest
     
      steps:
      #環境を設定する
        - name: create text
          uses: actions/checkout@v3
        - run: |
             echo "前処理"
             
      #チェック確認
        - name: check1
          run: |
             echo "check-bats-version1を実施しました"

    check-bats-version2:
      runs-on: windows-latest
      needs: check-bats-version1
     
      steps:
      #環境を設定する
        - name: create text
          uses: actions/checkout@v3
        - run: |
             echo "input_mod=${{inputs.input_mod}}"
             echo "input_compo=${{inputs.input_compo}}"

      #チェック確認
        - name: check2
          if: env.INPUT_MOD
          run: |
            echo "入力値が入ってます"

        - name: check3
          run: |
            echo "入力値が入ってません" 
