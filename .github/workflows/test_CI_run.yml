name: form-example 
on:
 workflow_dispatch:
    inputs:
      input_node:
        description: 'Please enter the node name.'
        required: false
 
jobs:
    check-bats-version:
      runs-on: windows-latest
     
      steps:
      #環境を設定する
        - name: create text
          uses: actions/checkout@v3
        - run: |

      #チェック確認
        - name: check1
          run: |
            if ("${{inputs.input_node}}" -ne "") {
             echo "空じゃないです"
            }else{
             echo "空です"
            }
            
